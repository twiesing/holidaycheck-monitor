{
  "name": "holidaycheck-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev:server": "pnpm --filter @holidaycheck-monitor/server dev",
    "dev:web": "pnpm --filter @holidaycheck-monitor/web dev",
    "start": "pnpm --filter @holidaycheck-monitor/server start"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
